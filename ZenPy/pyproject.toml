[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "zenpy"
version = "0.1.0"
requires-python = ">=3.10"
description = "Pure-Python cross-platform RyzenAdj reimplementation"

[project.scripts]
zenpy = "zenpy.cli:main"
